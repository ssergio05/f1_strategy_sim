# Real-time telemetry collector dependencies (scripts/live_tracker.py)
# Install via: pip install -r scripts/requirements-live.txt
requests>=2.28
websockets>=12.0

# ---------------------------------------------------------------------------
# OPTIONAL (NOT used for real-time predictions): 
# The fastf1 library is only required for offline analysis/replay of recorded sessions.
# Its fastf1.livetiming module only records the stream to be reprocessed later 
# via Session.load(livedata=...); it does not support live data extraction mid-session.
# fastf1>=3.6